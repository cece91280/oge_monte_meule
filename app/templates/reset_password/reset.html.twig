{% extends 'base.html.twig' %}

{% block title %}réinitialise mot de passe{% endblock %}

{% block body %}
    <h1>Réinitialise le mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">réinitialise</button>
    {{ form_end(resetForm) }}
{% endblock %}
